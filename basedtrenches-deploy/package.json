{
  "name": "basedtrenches-deploy",
  "version": "1.0.0",
  "description": "Based Trenches deployment scripts",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}
